[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.3",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
    "sqlmodel>=0.0.38",
    "uvicorn>=0.44.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]  # Le dice: "La raíz es esta misma carpeta"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
