{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
    "scripts": {
    "dev": "concurrently -n \"API,WEB\" -c \"blue,green\" --kill-others \"cd backend && uv run uvicorn app.main:app --reload\" \"cd frontend && ng serve\"",
    "sync": "concurrently -n \"API_SYNC,WEB_INSTALL\" -c \"blue,green\" --kill-others \"cd backend && uv sync\" \"cd frontend && npm install\""
  }
}
